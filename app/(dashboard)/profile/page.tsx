import { ProfileView } from "@/features/profile";

export const metadata = {
	title: "Profile | SiSantri",
	description: "View and manage your profile settings.",
};

export default function ProfilePage() {
	return <ProfileView />;
}
