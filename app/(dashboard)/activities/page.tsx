import { ActivityView } from "@/features/activity/components/activity-view";

export const metadata = {
	title: "Activities | Sisantri",
	description: "Manage organization activities",
};

export default function ActivitiesPage() {
	return <ActivityView />;
}
