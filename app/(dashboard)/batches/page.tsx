import { BatchView } from "@/features/batch/components/batch-view";

export const metadata = {
  title: "Batches | SiSantri",
  description: "Manage student batches in SiSantri.",
};

export default function BatchesPage() {
  return <BatchView />;
}
